export type TimelineEvent = {
    id: string,
    title: string,
    subtitle: string,
    description: string,
    image?: string,
    imageAlt?: string,
    imageWidth?: number,
    imageHeight?: number,
}
